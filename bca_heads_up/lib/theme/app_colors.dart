import 'package:flutter/material.dart';

// putting colors here so i dont have random hex codes everywhere
// easier to change the whole app later
class AppColors {
  static const Color background = Color(0xFF101820);
  static const Color card = Color(0xFF1F2A36);
  static const Color cardLight = Color(0xFF2D3A4A);
  static const Color accent = Color(0xFF7DD3FC);
  static const Color accent2 = Color(0xFFFFC857);
  static const Color textMain = Colors.white;
  static const Color textSoft = Colors.white70;
}